name: sift
description: Use the Sift entrypoints in PHP projects.
instructions:
  - Run supported tools through `composer sift`, `php vendor/bin/sift`, or `php sift.phar` by default unless the user opts out; manage agent skills with their `skills` command or `composer skills`.
